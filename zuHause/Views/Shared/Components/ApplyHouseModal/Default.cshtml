@using zuHause.ViewModels.MemberViewModel
@model ApplicationViewModel
<button type="button" class="btn btn-aqua py-0 col-auto" data-bs-toggle="modal"
        data-bs-target="#applyHouse">
    (房客)預約看房
</button>


<div class="modal fade" id="applyHouse" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
     aria-labelledby="applyHouseLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form asp-controller="MemberApplications" asp-action="CreateApplyHouse" method="post" class="w-100">
                <input type="hidden" name="ReturnUrl" value="@Context.Request.Path" />
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="applyHouseLabel">預約看房</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-3 d-flex flex-column gap-3">
                    @* <div class="row g-0 align-items-center gap-1">
                        <h6 class="col-auto m-0 me-2">是否提供聯絡電話：</h6>
                        <div class="form-check form-check-inline col-auto">
                            <input class="form-check-input" type="radio" name="inlineRadioOptions" id="inlineRadio1"
                                   value="option1">
                            <label class="form-check-label" for="inlineRadio1">是</label>
                        </div>
                        <div class="form-check form-check-inline col-auto">
                            <input class="form-check-input" type="radio" name="inlineRadioOptions" id="inlineRadio2"
                                   value="option2" checked>
                            <label class="form-check-label" for="inlineRadio2">否</label>
                        </div>
                    </div> *@
                    <div class="row g-0 align-items-center gap-1">

                        <h6 class="col-auto m-0 me-2">預約時間：</h6>
                        <div class="time-picker col-auto">



                            @* 請插入房源編號 *@
                            <input type="hidden" asp-for="PropertyId" value="@Model.PropertyId" />
                            <input type="date" class="border rounded-1" asp-for="ScheduleDate" min="@DateTime.Today.ToString("yyyy-MM-dd")">
                        </div>
                        <div class="col-auto">
                            <select class="form-select form-select-sm" aria-label="Small select example" asp-for="ScheduleHouse">
                                <option value="">時</option>
                                @for (int i = 6; i <= 23; i++)
                                {
                                    <option value="@(i.ToString().PadLeft(2, '0'))">@(i.ToString().PadLeft(2, '0'))</option>

                                }
                            </select>
                        </div>
                        <div class="col-auto">
                            <select class="form-select form-select-sm" aria-label="Small select example" asp-for="ScheduleMinute">
                                <option value="">分</option>
                                <option value="00">00</option>
                                <option value="15">15</option>
                                <option value="30">30</option>
                                <option value="45">45</option>
                            </select>
                        </div>
                    </div>
                    <div class="row g-0 align-items-center gap-1">

                        <h6 class="col-auto m-0 me-2">備註：</h6>
                        <div class="col">
                            <textarea asp-for="Message" class="w-100 form-control" rows="5" maxlength="300" placeholder="請輸入300字以內"></textarea>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
                    <button type="submit" class="btn btn-aqua">提交申請</button>
                </div>
            </form>
        </div>
    </div>
</div>