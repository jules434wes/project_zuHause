@{
    ViewBag.Title = "跑馬燈管理";
}

<link href="~/css/marquee.css" rel="stylesheet" />

<div class="container-fluid py-4">
    <div class="row">

        <!-- 主內容 -->
        <div class="col-md-10">
            <div class="panel">
                <ul class="nav nav-tabs mb-3" id="marqueeTabs">
                    <li class="nav-item">
                        <button class="nav-link active" data-scope="TENANT" onclick="changeMarqueeTab(this)">租客首頁</button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link" data-scope="LANDLORD" onclick="changeMarqueeTab(this)">房東首頁</button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link" data-scope="FURNITURE" onclick="changeMarqueeTab(this)">家具首頁</button>
                    </li>
                </ul>

                <h5 class="my-3">租客首頁跑馬燈預覽</h5>
                <div class="panel mt-4">
                    <div id="marquee-preview-wrapper" style="height: 30px; overflow: hidden; position: relative;">
                        <div id="marquee-line" class="position-absolute w-100 text-primary fw-bold text-center"></div>
                    </div>
                </div>


                <h6>執行中跑馬燈</h6>
                <table class="table table-bordered table-sm align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>播放排序</th>
                            <th>播放內容</th>
                            <th>圖片網址</th>
                            <th>播放開始時間</th>
                            <th>播放結束時間</th>
                            <th>啟用狀態</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody id="marquee-tbody">
                        <!-- 這裡由 JS 動態產生 -->
                    </tbody>
                </table>

                <h6 class="mt-4">新增跑馬燈</h6>
                <table class="table table-bordered table-sm align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>播放排序</th>
                            <th>播放內容</th>
                            <th>圖片網址</th>
                            <th>播放開始時間</th>
                            <th>播放結束時間</th>
                            <th>啟用狀態</th>
                            <th>確認新增</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr id="marquee-add-row">
                            <td><span id="next-order">1</span></td> <!-- 排序僅顯示 -->
                            <td><input type="text" class="form-control" id="new-content"></td>
                            <td><input type="text" class="form-control" id="new-attachment"></td>
                            <td><input type="datetime-local" class="form-control" id="new-start"></td>
                            <td><input type="datetime-local" class="form-control" id="new-end"></td>
                            <td><input type="checkbox" class="form-check-input" id="new-active" checked></td>
                            <td>
                                <button class="btn btn-success btn-sm" onclick="addNewMarquee()">確認</button>
                                <button class="btn btn-danger btn-sm" onclick="resetNewForm()">取消</button>
                            </td>
                        </tr>
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</div>
