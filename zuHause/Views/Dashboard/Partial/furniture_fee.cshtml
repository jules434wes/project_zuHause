
@{
}
<div class= "container mt-4" >
    <!-- 1. 目前費用狀態 -->
    <div class="container bg-light p-3 rounded border mb-4" id="activeDeliveryPlanContainer">
        <h5 class="mb-3 fw-bold">目前配送費設定</h5>
        <!-- 由 JS 動態插入 row -->
    </div>


    <div class="container bg-light p-3 rounded border mb-4">
        <h5 class="mb-3 fw-bold">已排程配送費設定</h5>
        <div id="scheduledDeliveryPlanContainer"></div>
    </div>

    <hr class="my-4">
    <div class="card mt-4">
        <div class="card-header bg-success text-white">新增配送費方案</div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-4">
                    <label for="planName" class="form-label">方案名稱</label>
                    <input type="text" class="form-control" id="deliveryPlanName" />
                </div>
                <div class="col-md-4">
                    <label for="baseFee" class="form-label">基礎費用</label>
                    <input type="number" class="form-control" id="baseFee" />
                </div>
                <div class="col-md-4">
                    <label for="remoteFee" class="form-label">偏遠地區費用</label>
                    <input type="number" class="form-control" id="remoteFee" />
                </div>
                <div class="col-md-4">
                    <label for="currencyCode" class="form-label">幣別</label>
                    <input type="text" class="form-control" id="deliveryCurrencyCode" value="NT$" />
                </div>
                
                <div class="col-md-4">
                    <label for="startAt" class="form-label">開始日期</label>
                    <input type="datetime-local" class="form-control" id="deliveryStartAt" />
                </div>
                <div class="col-md-4">
                    <label for="endAt" class="form-label">結束日期（可空）</label>
                    <input type="datetime-local" class="form-control" id="deliveryEndAt" />
                </div>
            </div>
            <div class="mt-3 d-flex align-items-center">
                <button class="btn btn-success" onclick="submitDeliveryPlan()">💾 儲存方案</button>
                <button id="cancelDeliveryEditBtn" class="btn btn-outline-secondary ms-2 d-none" onclick="clearDeliveryPlanForm()">❌ 取消編輯</button>
            </div>
        </div>
    </div>
    <div class="card mb-4">
        <div class="card-header">📋 配送費方案清單</div>
        <div class="card-body p-2">
            <table class="table table-bordered table-sm text-center" id="deliveryPlansTable">
                <thead class="table-light">
                    <tr>
                        <th>ID</th>
                        <th>名稱</th>
                        <th>基礎費用</th>
                        <th>偏遠費用</th>
                        <th>期間</th>
                        <th>狀態</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody><!-- 由 JS 動態填入 --></tbody>
            </table>
        </div>
    </div>
    <div class="toast-container position-fixed bottom-0 end-0 p-3" id="toastArea" style="z-index: 9999;"></div>

    
</div>